vim:
  base_dir:     ~/.vim
  autoload_dir: ~/.vim/autoload
  backups_dir:  ~/.cache/vim/backups
  swaps_dir:    ~/.cache/vim/swaps
  views_dir:    ~/.cache/vim/views
  undos_dir:    ~/.cache/vim/undos
  sessions_dir: ~/.cache/vim/sessions
  tmp_dir:      ~/.cache/vim/tmp

features:
  - vanilla


# vim: set filetype=eruby.yaml :
